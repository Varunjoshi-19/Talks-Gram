import React from "react";
import  styles from "../Styling/smallLoader.module.css";

const SmallLoader: React.FC = () => {
  return (
    <div className={styles.smallLoader}></div>
  );
};

export default SmallLoader;
