note -> If you want to run this project on your system then you must need node js and mongodb installed on your system 
        and then you can install the dependencies and node_module folder by using cmd -> [ npm install ] , after this
        your can run this application using cmd -> [ npm run dev ]  for frontend 
                                                    [ npm start ] for backend
